specVersion: "v1"
serviceId: "processAI"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "processai-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
